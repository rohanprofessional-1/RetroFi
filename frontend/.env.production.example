# Copy to .env.production and commit (used by Cloud Build + local production builds).
# This is a public URL, not a secret. Local-only overrides go in frontend/.env (gitignored).
VITE_API_BASE_URL=https://retrofi-backend-836379828951.us-east1.run.app
